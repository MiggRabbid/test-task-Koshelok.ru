#### Найденные пробелемы и внесенные исправления

##### 1. Неиспользуемые данные, функции и компоненты

- удалён "старый" компонент HelloWorld;
- неиспользуемые данные в App.vue (связанные с userTasks, в тз нет, что их нужно использовать);
- исправлены дублирующиеся функции formatDate и formatTaskDate;
- убраны неиспользуемые функции;
- убраны слушатели для handleKeyPress и handleResize. Либо нужно сделать для них:

```
  onUnmount(() => {
    window.removeEventListener('keypress', handleKeyPress)
    window.removeEventListener('resize', handleResize)
  })
```

##### 2. Не строгая типизация

- добавлены типы и вынесены в отдельный файл.

##### 3. Изменена архитектура

- приложение разбито на компоненты;
- в компонентах использована мемоизация для улучшения производительности;  
- fake-запрос loadTasks вынесен в отдельный файл;
- общие функции вынесены в отдельный слой utils (хотя их не много).

##### 4. Нет глобального хранилища

- добавлено Pinia, для лучшей работы с данными.

##### 5. Касательно безопасности

- для Лого заменён <object> на <img> и файл сохранён локально. Т.к. это более корректно и безопасно ~~~подвох был исправлен на автомате, но не сразу замечен :-)~~~;

- можно добавить защиту от атак типа XSS;
- подтверждение при удалении задач (чтобы не мискликнуть).

#### Новый функционал и фичи

##### 1. Работа с localStore

- добавлен хук useLocalStorage для сохранения данных локально.

##### 2. Логирование изменений и их отрисовка

- добавлено логирование изменений;
- добавлен стор useChangeLogStore для логирования;
- добавлена фича TasksLog.

#### Что еще можно улучшить

- более правильно переписать pinia сторы (в текущей реализации перенесены функции и ref`ы из компонента App.Vue);
- добавить валидацию в инпут создания новой задачи;
- более четко разделить на слои UI и логики (инпуты, кнопки и т.д.);
- убрать лишнеи шрифты при сборке проекта;
- добавить обработку ошибок для api.
